# START HERE — First commit. <Done>

1. [ ] Create a file `hello.py` in this folder with: `print("Hello, Git!")`
2. [ ] Run `git status` — you should see `hello.py` untracked
3. [ ] Run `git add hello.py` then `git commit -m "feat: add hello.py"`
4. [ ] Run `git log --oneline` — you should see your commit

Done? Check [expected-result.md](expected-result.md).
